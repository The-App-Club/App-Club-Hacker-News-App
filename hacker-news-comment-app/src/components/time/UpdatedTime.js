import styled from '@emotion/styled';

const StyledUpdatedTime = styled.span``;

const UpdatedTime = ({children}) => {
  return <StyledUpdatedTime>{children}</StyledUpdatedTime>;
};

export {UpdatedTime};
